/**
 * @dealer/web — buildable structural shell (FBL-010).
 *
 * No customer product UI, no framework selection beyond ADR-005, not part of the
 * production deployment topology in this order.
 */
export const WEB_APP = '@dealer/web';
