# FBL-020-R7 — order text, checked in verbatim

This file holds the complete FBL-020-R7 order exactly as received on 2026-08-21, per its own
§1 item 2. Nothing below the rule is edited, summarised, or reflowed beyond the transport's
own formatting. The reviewer's disposition preamble is retained because §0's authoritative
baseline figures restate it and a transcription that drops its own context is no longer a
transcription.

---

Good—the previous stop message should not be sent. I reviewed the actual ZIP, source, CI evidence, and blueprint copies. Send the new R7 order below instead.
Key review findings:

* The R6 source matches all 295 GitHub paths after LF normalization, but 292 text files were converted to CRLF. It is content-equivalent, not a byte-exact Git archive.
* [Run 32493409959](https://github.com/Alegnta19/Car-dealership-software/actions/runs/32493409959) is genuinely green, 4/4.
* The ZIP omits the four CI artifact ZIPs, run manifest, tree identity, and checksums. It is a source snapshot—not a self-contained evidence packet.
* Three runtime and seven database-control defects remain outside the passing tests.
* The repository contains the correct Version 2.0 blueprint, SHA-256 `556d4e10…`.
* Both supplied project copies are still byte-identical Version 1.0 files, SHA-256 `d38ad00a…`. That external condition remains open.

Disposition: R6 technical work is retained, but R6 is not accepted. R7 is the final bounded correction.
FBL-020-R7 — Final Identity-Boundary Integrity Correction
0. Governing disposition

```text
Authoritative baseline:
ee5eb6b00ef91542f3129fa9957fc22d3ce51f0f

Baseline tree:
6bc4302612abfc97df0854708ee576ba3108a616

Baseline CI:
run 32493409959 — SUCCESS, 4 of 4 jobs

FBL-020-R6:
TECHNICAL BASELINE RETAINED
NOT ACCEPTED
SUPERSEDED BY FBL-020-R7

FBL-020-R7:
ACTIVE

FBL-030:
BLOCKED

Live WorkOS certification:
UNDISCHARGED
```

The Version 2.0 management-first blueprint is correctly committed in the repository at:
`docs/orders/Car_Dealership_Management_and_Sales_Cloud_Master_Blueprint.docx`
Its SHA-256 is:
`556d4e108c9db8b7dcfee284828f926157f7663d260d3d3e0d8774bb032feaaf`
Reviewer inspection has now established that both supplied external project copies remain Version 1.0, SHA-256 `d38ad00ad2cd5a13ac087dbb96a34a4c133d0e5bfe8c81d9820a0b69f31e03f9`. Do not claim that those copies were updated. Do not spend implementation time trying to change records outside the repository.
The migration decision is final: migrations `000` and `049`–`058` are frozen. Do not conduct another census. Every R7 schema correction belongs in new migration:
`migrations/059_policy_evidence_integrity_closure.sql`
1. Delivery discipline

1. Create branch `fbl-020-r7` from the exact baseline SHA. Do not work from the supplied Atlas ZIP because its text files were converted to CRLF.
2. Check this complete order into `docs/orders/FBL-020-R7.md` verbatim.
3. Do not push changes directly to `main`.
4. Local checkpoint commits are permitted, but before the first push they must be squashed into exactly one code-bearing R7 commit.
5. Push that one commit to the R7 branch and open a draft pull request against `main`.
6. Run the existing pull-request CI against that exact SHA.
7. If CI is red, stop and return the failure evidence. Do not autonomously amend, push another commit, or begin another audit cycle.
8. If CI is green, do not merge. Assemble the external evidence packet, return it for architect review, and stop.
9. No documentation-only closeout commit is allowed.
10. Do not run a sixth narrative audit. Use deterministic gates, named tests, and mutation proofs.

Do not rebuild the R6 controls already working: handle-first lifecycle routing, atomic session/custody/success commit, MFA tenant locking, worker `--once` failure propagation, migration 058 reconciliation, and current fixture-chain protections must be preserved.
2. Runtime corrections
2.1 Expiry must use actual database completion time
In `succeedLoginTransactionWithin`, transaction-start `NOW()` is not completion time.
Required behavior:

* Obtain one fresh database wall-clock instant at the success transition, using `clock_timestamp()` or an equivalent database-owned live clock.
* Use that same instant for the success predicate and `consumed_at`.
* Use live database time when classifying a refused update as expired.
* Do not use JavaScript time.
* If completion occurs at or after `expires_at`, the transaction must return `expired`.
* No session, refresh custody, session-established audit, or login-success audit may survive that refusal.
* Preserve the existing single-transaction custody design.

Add the named database-backed test:
`login completion refuses after DB wall time crosses expiry inside the already-open admission transaction`
The test must prove that transaction `NOW()` remains before expiry while the live database clock has crossed it.
2.2 Missing purpose must be terminal
Remove both nullish defaults:

* `presentedPurpose ?? input.purpose`
* `presentedPurpose ?? 'reauth'`

For login and reauthentication:

* `presentedPurpose` is explicitly `string | null`.
* Missing, non-string, or wrong purpose terminalizes as `callback_purpose_mismatch`.
* The real route must forward a valid handle into the lifecycle service; the route may not pre-screen the defect.
* Unknown or malformed handles remain neutral refusals without fabricated rows or audits.
* A later correct callback loses as replay.

Add real-route tests for missing purpose on both login and step-up callbacks.
2.3 Reauthentication terminal reasons must be truthful
Add and use:

* `callback_pkce_mismatch`
* `callback_redirect_mismatch`

Classification must be exact:

* missing/wrong state → `callback_state_mismatch`
* missing/wrong verifier → `callback_pkce_mismatch`
* wrong callback URI → `callback_redirect_mismatch`
* missing/wrong purpose → `callback_purpose_mismatch`

Each failure produces one failed terminal state and exactly one existing `identity.reauthentication.failed` audit event. Do not record presented state, verifier, nonce, cookie, code, token, or provider data.
Update:

* `tests/login-admission.test.ts`
* `tests/reauth-callback-lifecycle.test.ts`
* `tests/identity-lifecycle-audit.test.ts`
* legitimate direct claim callers in the test kit and existing identity tests

3. Database-control corrections
3.1 Support request, session, actor, and tenant must be one tuple
Migration 059 must:

* Precheck retained request/session actor mismatches and refuse with bounded row identifiers. Do not rewrite evidence.
* Establish a unique target over `(request_id, tenant_id, requester_user_link_id)`.
* Bind support sessions to that exact request/tenant/actor tuple.
* Require support requesters and support-session actors to be real `user_links` with `actor_scope='platform'`.
* Bind `policy_decisions.actor_type='platform_support'` to the real actor scope instead of trusting a caller-supplied label.

Required tests:

* `a support session cannot substitute another REAL platform actor for the approved requester`
* `a dealership link cannot be recorded as a platform-support actor`

Each requires a valid same-actor/platform control leg.
3.2 A session may not exceed or precede its approval
Make request authority fields immutable: tenant, requester, actions, scope, duration, and approval identity.
Make session authority fields immutable: request, tenant, actor, granted time, and expiry.
Require:

* `session.granted_at >= request.decided_at`
* session expiry does not exceed the specifically requested duration
* the approval grant names the same request, tenant, decider, action `identity.support.approve`, resource type `support_access_request`, resource ID, required assurance, and MFA certification
* every non-tenant approved scope exists, belongs to the request tenant, and has an active/effective ancestor chain

Required negative tests include:

* one-minute approval cannot produce a longer session
* session cannot begin before approval
* wrong-action and wrong-resource real grants cannot approve
* nonexistent scope
* foreign-tenant scope
* inactive or out-of-window scope
* inactive or out-of-window ancestor

Include a valid active-scope control.
3.3 Remove the name-based `platform.*` bypass
An action name must never decide whether customer-tenant delegation is required.
For current evidence:

* Every platform-support ALLOW with a non-null authorization tenant requires delegated-support evidence, regardless of action prefix.
* A genuine control-plane decision is accepted only when it is structurally platform-scoped, has a null authorization tenant, carries no customer-resource payload, and names valid platform authority.
* If target-tenant metadata is operationally required for a control-plane action, store it separately from the authorization-bound tenant.

Required test:
`a platform-prefixed action cannot carry customer resource evidence without delegation`
Include a genuine resource-less platform control-plane control.
3.4 Resource decisions must be tied to their organization ancestry
Remove the current resource-scope hierarchy exemption.
Required design:

* Use one database-authoritative resource-scope resolver or transactionally maintained resource-scope registry.
* The runtime resolver and the evidence validator must use the same authority.
* Persist the database-validated organization leaf/snapshot used at the decision.
* Validate every matched organization binding against that resource’s actual leaf and ancestry.
* Do not trust caller-supplied resolved-scope columns without database validation.

Required tests:

* rooftop-A binding cannot authorize a real rooftop-B resource
* ancestor binding can authorize a real descendant resource
* exact-resource binding requires exact resource type and ID
* nonexistent and foreign-tenant resources are rejected

3.5 Effective authority must include the tenant and full organization chain
Establish one database authority for effective ancestry and use it in both runtime authorization and evidence validation.
At actual evidence-write time, validate:

* tenant active status and effective window
* organization leaf active status and effective window
* every ancestor’s active status and effective window
* role-binding status and effective window

Required negative tests:

* inactive tenant
* future-effective or expired tenant
* inactive leaf
* future-effective or expired leaf
* inactive ancestor
* future-effective or expired ancestor

Include a complete active-chain control.
3.6 Authorization liveness must use actual write time
Do not use transaction-start `NOW()` or caller-controlled/backdated `occurred_at` to prove authority was live when evidence was inserted.
Required behavior:

* Role bindings must be live at the actual database write instant.
* Support sessions must be live at the actual database write instant.
* Recorded occurrence time may be checked for consistency, but it cannot replace the live insertion-time check.
* A transaction started before authority expiry and completed afterward must be refused.
* An expired support session combined with backdated `occurred_at` must be refused.

3.7 Replace the forgeable GUC writer guard
`policy_evidence.normalizing_decision` is caller-forgeable and is not writer authorization.
Required correction:

* Remove the GUC as authorization.
* Separate migration-owner and runtime database roles.
* API and worker must use the non-owner runtime role.
* Revoke direct child-table DML from the runtime role.
* Normalize through a hardened `SECURITY DEFINER` trigger/function owned by a role the runtime role cannot assume, with a fixed safe `search_path`.
* Preserve exact parent/child equivalence as an independent invariant.

Required test:

* As the actual runtime role, setting any forged GUC and directly inserting an otherwise exact child must fail with SQLSTATE `42501`.
* Inserting the parent as that runtime role must still cause database-owned normalization.

4. Migration rules

* Add migration 059 only.
* Migrations `000` and `049`–`058` must remain byte-immutable.
* Use a new evidence version for current R7 evidence when necessary; preserve versions 1–3 without rewriting historical decisions or inventing approvals.
* Add an exact-checksum `pre-059` fixture chain.
* The populated upgrade drill must:
   1. build and use a real 058 database;
   2. generate valid post-058 activity through production paths;
   3. apply 059;
   4. verify every new invariant;
   5. prove invalid retained rows cause the intended bounded precheck refusal;
   6. prove fresh and upgraded fingerprints converge.
* Every migration refusal must give an actionable, truthful explanation. Do not instruct an operator to modify append-only evidence.

5. Tests, mutations, and gates
Preserve at least 654 tests across 64 suites, plus all new named tests, with zero failed, cancelled, skipped, or todo tests and zero anchored `not ok`.
Preserve the current quality ratchet at or below `53/123/1`. Do not raise a baseline.
Add independently killed runtime mutations:

1. `login_completion_uses_transaction_start_time`
2. `login_missing_callback_purpose_assumed`
3. `login_route_rejects_missing_purpose_before_lifecycle`
4. `stepup_missing_callback_purpose_assumed`
5. `stepup_route_rejects_missing_purpose_before_lifecycle`
6. `stepup_pkce_mismatch_mislabeled_as_state`
7. `stepup_redirect_mismatch_mislabeled_as_state`

Add one database mutation for every atomic predicate introduced above. Do not group several predicates into one mutation. Each mutation must name the exact test expected to die.
All mutation baselines must be green before mutation. Zero survivors is required.
The mutation artifact must report `tree_dirty=false`. Move temporary server PID/state outside the repository or clean it before provenance measurement.
Required green gates:

* locked install
* typecheck
* ratchet
* format
* architecture
* build
* complete database-backed test battery
* runtime mutations
* database-control mutations
* populated 058→059 upgrade drill
* fresh/upgrade fingerprint equality
* dependency audit
* SBOM
* container build
* full-history secret scan
* evidence completeness

6. Documentation controls
Create:

* `docs/orders/FBL-020-R7.md` — this order verbatim
* `docs/FBL-020-R7-REQUIREMENT-MAP.json`

The requirement map must identify the exact implementation, test, and mutation for every numbered clause.
Keep the current R6 order’s missing-text disclosure truthful. Do not fabricate unavailable R6 wording.
Use one canonical machine-readable state record. Other documents must reference it rather than restating moving SHAs, run numbers, counts, or distances.
The checked-in R7 state must say:

```text
SUBMITTED FOR REVIEW — NOT ACCEPTED
```

It must not claim that the final exact-SHA run already exists inside the commit that triggers it. The external return packet binds the final commit to its run.
Preserve these external blockers without expanding them:

* both supplied project copies remain Version 1.0
* live WorkOS certification remains undischarged
* prior commit-budget violations remain disclosed and are not cured
* FBL-030 remains blocked

Do not perform another general prose audit. Run the deterministic documentation gates once after the implementation is settled.
7. Final evidence packet and stop condition
Do not first create a repaired R6 packet. Produce one R7 packet after the R7 exact-SHA CI run.
Generate the source archive directly from the final Git object using `git archive`, with a top-level directory prefix. Do not zip a Windows working tree.
The packet must contain:

1. Exact Git-object source archive.
2. All four downloaded CI artifact ZIPs.
3. `RUN-MANIFEST.json` containing:
   * repository
   * baseline SHA
   * final commit SHA
   * final tree SHA
   * pull-request URL
   * workflow path
   * run ID and URL
   * every job ID, name, status, and conclusion
   * every artifact ID, name, size, and SHA-256
   * source archive SHA-256
   * order-file SHA-256
   * unresolved external blockers
4. `SHA256SUMS` covering every delivered file.
5. A short return report that cites the manifest rather than retyping its moving figures.

Verify that the archive’s paths, modes, and raw blob bytes reproduce the final Git tree. LF-to-CRLF normalization is not acceptable.
Return only:

* branch and draft pull-request URL
* final commit and tree SHA
* exact-SHA run URL and four job conclusions
* packet filename and SHA-256
* manifest and artifact identities
* named test/mutation results
* open external blockers

Then stop. Do not merge, begin FBL-030, create a closeout commit, or start another audit.
