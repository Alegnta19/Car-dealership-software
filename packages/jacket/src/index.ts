/**
 * @dealer/jacket — FBL-140: deal jacket, documents and e-sign evidence.
 *
 * The jacket opened FROM one exact manager-approved desking version through
 * the seam `@dealer/desking` exposes; the checklist resolved from versioned,
 * effective-dated configuration; the package assembled from canonical records
 * with every figure the approved version's own integer cents; the documents
 * rendered deterministically into content-addressed, immutable artifacts; the
 * ceremony that binds signers, consent, intent and signatures to exactly one
 * package hash; and the lanes that keep a salesperson, a manager, a customer,
 * an administrator, the provider and the clock from acting as one another.
 *
 * This package owns migration 066's tables and nothing else. It declares no
 * sale, funding, delivery, sold vehicle, accounting entry or revenue event —
 * the later F&I and funding phases own those — and it invents no legal form:
 * a template records where its text came from and whether anybody accountable
 * approved it, and a sample says so on every page.
 */
export * from './hashing';
export * from './provider';
export * from './configuration';
export * from './intake';
export * from './checklist';
export * from './render';
export * from './assembly';
export * from './lifecycle';
export * from './ceremony';
export * from './signing';
export * from './callbacks';
export * from './reconcile';
export * from './holds';
export * from './reads';
export * from './actions';
