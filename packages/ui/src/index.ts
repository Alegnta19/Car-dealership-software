/**
 * @dealer/ui — UI primitives boundary (FBL-010 structural shell).
 *
 * No product screens or business workflows live here yet; this package exists so the
 * web app has a declared, dependency-checked home for UI foundations when they arrive.
 */
export const UI_PACKAGE = '@dealer/ui';
