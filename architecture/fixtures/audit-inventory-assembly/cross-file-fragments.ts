/**
 * SUPPORT FILE for `evasion-j-cross-file-fragment.ts`. It holds FRAGMENTS, not names:
 * on its own it trips nothing, which is the point — the name only exists once the
 * importing file joins these to its own leaf.
 */
export const NAMESPACE_ROOT = 'identity.';
export const SUPPORT_FAMILY = 'support';
